import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.kyawthuaung.marketingsupport', // <<< ဒီမှာထည့်ပါ
  appName: 'Marketing Support',
  webDir: 'build',
  server: {
    androidScheme: 'https'
  }
};

export default config;
